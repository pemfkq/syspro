#include <stdio.h>
#include <stdlib.h>

struct node {
	int data;
	struct node *next;
};

void push(struct node *top, int data);
{
	struct node *next = *top;
	struct data = data;
}

int pop(struct node *top);
